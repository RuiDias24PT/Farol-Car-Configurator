import { expect, test } from 'vitest'

// Placeholder so `vitest run` has at least one file until the real
// suites land in M2. Delete once constraints.test.ts exists.
test('the test harness runs', () => {
  expect(true).toBe(true)
})
